/* 
    This file is the common enviroment config that are consistent across all tracks. These 
    values are spread into the other env files so we don't have to add it to multiple 
    files when add/edditing.
*/

export const commonEnvironment = {
    buildNumber: '0.0.0',
    production: false,
    logErros: true,
    importantKey: ''
}